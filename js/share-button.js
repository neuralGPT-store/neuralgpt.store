/** * Botón flotante de compartir universal * Usa Web Share API en móvil, fallback en desktop */
(function() { 'use strict'; // Crear botón flotante solo si no existe ya uno de compartir visible function initShareButton() { // Si estamos en listing.html y ya hay botones de compartir, no duplicar if (window.location.pathname === '/listing.html' && document.querySelector('[onclick*="shareOn"]')) { return; } const btn = document.createElement('button'); btn.id = 'global-share-btn'; btn.className = 'share-float-btn'; btn.setAttribute('aria-label', 'Compartir esta página'); btn.innerHTML = ` <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"> <circle cx="18" cy="5" r="3"></circle> <circle cx="6" cy="12" r="3"></circle> <circle cx="18" cy="19" r="3"></circle> <line x1="8.59" y1="13.51" x2="15.42" y2="17.49"></line> <line x1="15.41" y1="6.51" x2="8.59" y2="10.49"></line> </svg> `; btn.onclick = handleShare; document.body.appendChild(btn); } function handleShare() { const url = window.location.href; const title = document.title; const text = document.querySelector('meta[name="description"]')?.content || title; // Intentar Web Share API (móviles principalmente) if (navigator.share) { navigator.share({ title: title, text: text, url: url }).catch(err => { // Usuario canceló o error console.log('[Share] Compartir cancelado o error:', err); }); } else { // Fallback desktop: mini popup showSharePopup(url, title); } } function showSharePopup(url, title) { // Eliminar popup anterior si existe const existing = document.getElementById('share-popup'); if (existing) existing.remove(); const popup = document.createElement('div'); popup.id = 'share-popup'; popup.className = 'share-popup'; popup.innerHTML = ` <div class="share-popup-header"> <span style="font-weight:600">Compartir</span> <button class="share-popup-close" onclick="document.getElementById('share-popup').remove()" aria-label="Cerrar">×</button> </div> <div class="share-popup-content"> <button class="share-popup-btn whatsapp" onclick="shareToWhatsApp('${escapeJs(url)}', '${escapeJs(title)}')"> <span style="font-size:1.4rem">💬</span> WhatsApp </button> <button class="share-popup-btn twitter" onclick="shareToTwitter('${escapeJs(url)}', '${escapeJs(title)}')"> <span style="font-size:1.4rem">𝕏</span> X (Twitter) </button> <button class="share-popup-btn copy" onclick="copyUrlToClipboard('${escapeJs(url)}')"> <span style="font-size:1.4rem">🔗</span> Copiar enlace </button> </div> `; document.body.appendChild(popup); // Cerrar al hacer clic fuera setTimeout(() => { popup.addEventListener('click', (e) => { if (e.target === popup) popup.remove(); }); }, 100); } window.shareToWhatsApp = function(url, title) { window.open(`https://wa.me/?text=${encodeURIComponent(title + ' - ' + url)}`, '_blank'); document.getElementById('share-popup')?.remove(); }; window.shareToTwitter = function(url, title) { window.open(`https://twitter.com/intent/tweet?url=${encodeURIComponent(url)}&text=${encodeURIComponent(title)}`, '_blank'); document.getElementById('share-popup')?.remove(); }; window.copyUrlToClipboard = function(url) { navigator.clipboard.writeText(url).then(() => { const popup = document.getElementById('share-popup'); if (popup) { popup.innerHTML = '<div style="padding:20px;text-align:center;color:var(--accent4)">✓ Enlace copiado</div>'; setTimeout(() => popup.remove(), 1500); } }).catch(err => { alert('No se pudo copiar el enlace'); }); }; function escapeJs(str) { return String(str || '').replace(/\\/g, '\\\\').replace(/'/g, "\\'").replace(/"/g, '\\"'); } // Inicializar cuando el DOM esté listo if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initShareButton); } else { initShareButton(); }
})();
