document.addEventListener('mousemove',e=>{
  const h=document.querySelector('.hero-cinema');
  if(!h) return;
  const x=(e.clientX/window.innerWidth-0.5)*10;
  const y=(e.clientY/window.innerHeight-0.5)*10;
  h.style.backgroundPosition=\\% \%\;
});
